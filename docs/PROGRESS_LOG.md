# 进度记录

本文档按周记录团队进展，不做每日流水账。每周例会或阶段合并后更新一次。

## 状态标记

- `Todo`: 尚未开始。
- `Doing`: 正在进行。
- `Blocked`: 被问题卡住。
- `Review`: 等待检查或合并。
- `Done`: 已完成并验证。

## 2026-05-31 to 2026-06-07

### 本周目标

- 初始化 Godot Demo 骨架。
- 建立 GitHub 远程仓库和协作规范。
- 确认美术与 VFX 前期工作流。
- 建立文档治理、进度记录和更改日志规则。
- 启动“源”初始匕首战斗切片。
- 启动全路线动态背景 props/tiles 拆分。

### 已完成

- 创建 Godot 4.6.x Demo 项目骨架。
- 建立 `main`、`develop`、`art/player-concept-pass` 分支。
- 创建公开仓库 `zixuanjiang332/Source`。
- 编写程序规范、GitHub 流程、素材交付、AI 辅助素材规则。
- 产出主角“源”三阶段 AI 概念草图及无武器版本，并登记到素材清单。
- 合入主菜单 UI 和武器轮廓概念参考。
- 完成初始匕首三段普攻、Demo 能量技、HUD 武器/能量/连段反馈和击杀目标推进。
- 搭建动态背景通用节点和实验室/城市背景分层结构。
- 登记完整动态背景氛围包的源文件路径、导出路径和规格。
- 完成实验室首批 AI final 背景 tiles/props，生成 58 张单帧 PNG 并拼接运行时 spritesheet。
- 生成初始商店 1920x1080 AI final 背景图，定位为出生点相邻维修补给间，暂未接入运行时场景。
- 更新 AI 素材规则，允许 AI 生成图、动作帧和 spritesheet 直接作为 final 素材接入。
- 将当前主角、主菜单 UI、武器概念图全量导出到 `assets/`，并接入主菜单、玩家显示、HUD 头像和匕首图标。
- 修复主菜单：切换到脸部主题图，点击开始前不加载关卡、HUD、VFX 或玩家。
- 接入“源”早期克隆体 66 帧动作 spritesheet，补齐玩家动作状态、地图边界和大视距相机。
- 将项目切换到 1920x1080 渲染基准，主菜单与 HUD 改为清晰的独立 UI 组件。
- 生成并接入清洁版主菜单背景、重生实验室背景和 4 张连续城市主关卡背景。
- 拆分重生实验室段和城市主战斗段，电梯改为播放转场后加载主关卡。
- 生成并接入“源”电子光刀终结技首批 VFX、角色重击动作帧和 WAV 音效。
- 实装满能量长按 `K` 的 7 蓝斩 + 1 红斩终结技序列，短按 `K` 保留 Flash Step。
- 将当前默认近战主线切换为拳头开局，并补上拳头动作标签、出图规格和动画回退接线。
- 补充拳头动作生产包，细化到逐动作、逐关键帧、逐提示词，方便下一轮直接出图。
- 增补逐帧提示词和筛图审核表，方便拳头连段按帧出图并统一评审。
- 增补逐帧 shotlist，便于跟踪拳头每一帧的候选、返修和接入进度。
- 增补拳头动作一致性锁、Batch 01 最终提示词，以及新帧落地后的运行时构建/部署脚本。
- 生成并实装一版过渡拳头动作集：由旧战斗帧去刀光、重排、拼条后接入 `combat_* / punch_*`，并清理旧 `atk_* / skill` 逐帧资源。
- 生成正式拳头条带模板 PNG，固定帧宽、基准线、中心线和帧序，便于后续直接重绘高质量正式帧。
- 生成过渡拳头动作 paintover guide，便于直接在当前 runtime 节奏上覆画正式拳头帧。
- 生成拳头关键帧看板与逐张重绘任务单，把正式高质量替换优先级收敛到少数关键帧。
- 继续把正式拳头重绘压缩到 Batch 01 的 9 帧，并补上逐帧最终提示词。
- 为 Batch 01 补上一张 3x3 视觉参考提示词，方便先统一拳头连段的重量层级。
- 为 Batch 01 补上替换脚本和替换清单，后续正式重绘完成后可直接压回 strip 并部署。
- 生成并落地两张拳头视觉参考图，作为 Batch 01 正式重绘前的力量方向参考。
- 补充“源”主角外貌锁定提示词，并将“只有右臂机械、左臂保持人类手臂”的硬约束写入关键帧和逐帧出图提示。
- 新增 Batch 02，把 `punch_skill` 的 `01 / 03 / 06 / 08` 四帧替换为单机械右臂版本，并重新接入 runtime。
- 将武器概念 sheet 拆分为 6 张独立透明武器 PNG，并登记到素材清单。
- 修复拳头 runtime 的裁剪串帧问题，重新构建 `player_yuan_runtime_frames.tres`，当前运行时审计为 `Suspect frames: 0`。
- 拉取并合入远端新增的武器拾取、商店和作坊/关卡传送流程，保留新玩法链路。
- 移除当前开局武器上的旧版终结技依赖，当前拳头主线只保留三段普攻和 `Drive Step`。

### 当前阻塞

- 当前部分 AI 参考帧仍存在双机械臂倾向，后续出图必须统一改用 `YUAN_APPEARANCE_LOCK.md`。
- 当前战斗切片仍需 Godot GUI 手动试玩确认手感。
- 城市动态背景主图已替换为连续大背景，局部动态 props 仍使用 placeholder sheet 驱动，正式 spritesheet 待后续处理。

### 下周计划

- 整理世界观和武器策划到 `docs/`。
- 产出主角轮廓草案、第一把武器草案、基础命中/冲刺特效方向。
- 确认第一版可录屏路线的场景节奏。
- 对新拳头 runtime 做 GUI 手动试玩，确认战斗节奏、碰撞和位移手感。
- 为开局拳头补一张正式 HUD icon，并把武器面板视觉补齐。
- 继续按生产包打磨关键帧，再补全后续更高质量条带替换。
- 将正式背景 tiles/props 分批替换到 `assets/pixel/background/`。

### 任务板

| 任务 | 负责人 | 状态 | 备注 |
|---|---|---|---|
| 项目骨架初始化 | Codex / 程序 | Done | 已推送远程仓库 |
| GitHub 协作流程 | Codex / 团队 | Done | `main` 已保护 |
| 文档治理规则 | Codex | Done | 已加入核心文档和校验 |
| 主角视觉草案 | 美术/VFX | Review | 已导出到 `assets/` 并接入玩家/HUD/动作帧，待 GUI 检查观感 |
| 武器视觉草案 | 策划 / 美术 | Review | 已导出到 `assets/`，完成 6 张透明武器拆分，并接入初始匕首 HUD 图标 |
| 初始拳头玩法切片 | Codex / 程序 | Review | 已切换默认武器资源、接入高质量拳头 runtime 资源、修复裁剪并移除旧终结技依赖，待 GUI 手动试玩 |
| 初始匕首玩法切片 | Codex / 程序 | Review | 已接入玩家动作帧、大视距相机和地图边界，待 GUI 手动试玩 |
| 动态背景框架 | Codex / 程序 | Review | 已改为 1920x1080 连续主背景，待 GUI 检查视觉节奏 |
| 实验室背景 AI final 素材 | Codex | Review | 重生段已接入高清背景，待 GUI 视觉验收 |
| 初始商店背景 AI final 素材 | Codex | Review | 已生成 1920x1080 单图，并接入重生点 -> 作坊 -> 主关卡流程与商店/传送交互；待试玩验收 |
| 城市背景 tiles/props 正式素材 | 美术/VFX | Doing | 主关卡 4 张连续背景已接入，局部动态 props 待替换 |
| 菜单与 HUD 清晰度修复 | Codex | Review | 菜单组件化和 1080p HUD 已实现，待 GUI 交互验收 |
| 电子光刀终结技 | Codex / 美术/VFX | Review | VFX/SFX/角色重击帧和长按 K 逻辑已接入，待 GUI 手感验收 |
| 拳头动作帧第二轮出图 | 美术/VFX | Doing | 目标是更强打击感和更顺滑重心转移，按 `PLAYER_ANIMATION_SPEC.md` 与 `FIST_ANIMATION_PRODUCTION_PACK.md` 制作 |
| 世界观文档整理 | 策划 | Todo | 建议新增 `WORLDVIEW.md` |

## 周报模板

复制以下模板新增到文件顶部最近日期下方：

```md
## YYYY-MM-DD to YYYY-MM-DD

### 本周目标

- 

### 已完成

- 

### 当前阻塞

- 

### 下周计划

- 

### 任务板

| 任务 | 负责人 | 状态 | 备注 |
|---|---|---|---|
|  |  | Todo |  |
```
